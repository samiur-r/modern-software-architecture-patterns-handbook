{
  "name": "serverless-ts-app",
  "type": "module",
  "scripts": {
    "deploy": "serverless deploy",
    "remove": "serverless remove"
  },
  "dependencies": {
    "aws-lambda": "^1.0.7"
  },
  "devDependencies": {
    "serverless": "^3.0.0",
    "serverless-esbuild": "^1.32.2",
    "esbuild": "^0.17.0",
    "typescript": "^5.0.0"
  }
}